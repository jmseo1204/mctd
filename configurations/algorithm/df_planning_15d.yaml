defaults:
  - df_planning

# 15D qpos-only antmaze: plan uses qpos(x,y,z,quat,joints); action via DQL+HILP with zero-padding to 29D
diffusion:
  architecture:
    network_size: 128
    num_layers: 8
    attn_heads: 4
    dim_feedforward: 512
